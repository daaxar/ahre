{
  "name": "ahre-cli",
  "version": "0.8.2",
  "description": "AhRE deterministic capability engine with minimal generation and explicit optional suggestions",
  "type": "module",
  "bin": {
    "ahre": "./bin/ahre.mjs"
  },
  "scripts": {
    "start": "node ./bin/ahre.mjs",
    "smoke": "node ./bin/ahre.mjs find \"http service\" --json && node ./bin/ahre.mjs help && node ./bin/ahre.mjs doctor --json",
    "check": "node --check ./src/ahre-cli.mjs && node --check ./bin/ahre.mjs"
  },
  "keywords": [
    "architecture",
    "recipe-engine",
    "cli",
    "ddd",
    "clean-architecture",
    "agents"
  ],
  "license": "MIT",
  "dependencies": {
    "ts-morph": "^24.0.0"
  }
}
