{
  "name": "ahre-cli",
  "version": "0.8.1",
  "description": "AhRE deterministic capability engine with a minimal public CLI and composable filesystem definitions",
  "type": "module",
  "bin": {
    "ahre": "./bin/ahre.mjs"
  },
  "scripts": {
    "start": "node ./bin/ahre.mjs",
    "smoke": "node ./bin/ahre.mjs find \"http service\" --json && node ./bin/ahre.mjs help && node ./bin/ahre.mjs doctor --json",
    "check": "node --check ./src/ahre-cli.mjs && node --check ./bin/ahre.mjs"
  },
  "keywords": [
    "architecture",
    "recipe-engine",
    "cli",
    "ddd",
    "clean-architecture",
    "agents"
  ],
  "license": "MIT",
  "dependencies": {
    "ts-morph": "^24.0.0"
  }
}
