{
  "name": "ahre-cli",
  "version": "0.4.0",
  "description": "AhRE - ArcHitecture Recipe Engine CLI with recipes, AST patching, installable skills, semantic inventory, graph, search, build planning, and minimal GitHub installer and .agents skill bootstrap",
  "type": "module",
  "bin": {
    "ahre": "./bin/ahre.mjs"
  },
  "scripts": {
    "start": "node ./bin/ahre.mjs",
    "smoke": "node ./bin/ahre.mjs intents list --json && node ./bin/ahre.mjs skill list --all --json && node ./bin/ahre.mjs recipe plan entity.capability.ensure --entity User --context Users --json && node ./bin/ahre.mjs graph build --json",
    "check": "node --check ./src/ahre-cli.mjs && node --check ./bin/ahre.mjs"
  },
  "keywords": [
    "architecture",
    "recipe-engine",
    "cli",
    "ddd",
    "clean-architecture",
    "agents"
  ],
  "license": "MIT",
  "dependencies": {
    "ts-morph": "^24.0.0"
  }
}
